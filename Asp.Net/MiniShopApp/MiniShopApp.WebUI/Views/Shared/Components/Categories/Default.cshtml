@model List<Category>

<div class="list-group">
    <a href="#" class="list-group-item list-group-item-action">Tümünü Göster</a>

    @foreach (var item in Model)
    {
        <a href="#" class="list-group-item list-group-item-action">@item.Name</a>
    }
</div>